# 设计文档 (design.md) — 3.super_plane

> 引擎：Godot 4（配合 Godot MCP 开发闭环）
> 版本：v1 设计草案，待用户确认

## 0. 一句话概述

一款 **Godot 4** 制作的 3D **硬核仿真**空战游戏：真实飞行气动、过载/失速、HUD 仪表，通过**任务关卡制**推进（拦截、护航、击毁目标与 BOSS）。

## 1. 游戏支柱（Pillars）

1. **硬核飞行模型**：玩家飞机的操控真实、有深度 —— 速度与能量的管理决定胜负。
2. **任务感**：每个关卡有明确的军事目标与简报，打完有结算。
3. **沉浸 HUD**：高度、速度、航向、G 值、雷达/目标指示、威胁告警、导弹锁定，全部仪表化呈现。

## 2. 范围界定（重要）

### ✅ 属于 v1（本设计范围）

| 模块 | 说明 |
|---|---|
| 玩家可选 1 架飞机 | 飞行模型为本作核心 |
| 硬核飞行模型 | 带俯仰/横滚/偏航阻尼、推力、重力、简化升力模型；低速/高攻角**失速**；**G 力限制**（黑视/红视视觉反馈）；能量管理（高度↔速度互换） |
| 飞控辅助 | 可选 G 限幅器 / 攻角限幅（保持"硬核但可玩"） |
| 武器系统 | 机炮（弹药/过热）+ 导弹（锁定→发射→命中）+ 干扰弹（铝箔/热焰） |
| 敌机 AI | 分难度：追击、规避、反制干扰弹 |
| 玩家损伤模型 | 结构损伤 → 影响操控 → 击毁 |
| HUD | 高度、速度、航向、G 表、雷达扫描、锁定指示、威胁告警、任务目标文本 |
| 任务关卡制 | 至少 3 关（拦截 → 护航 → BOSS），含简报与结算界面 |
| 菜单/暂停/重启 | 基础 UI |
| 音效与基础音乐 | 由免费/程序生成音效起步 |

### ❌ 不在 v1（后续版本再议）

- 完整航电系统（真实雷达模式、INS 航线规划、航电开关面板）
- 多机型与武器挂载自定义
- 联网多人
- 地形复杂气象系统、真实机场地勤流程
- 战役/剧本系统（先做单个任务序列）

> **范围裁定**：走"**硬核飞行模型 + 游戏化作战系统**"路线，而非 DCS 级全系统仿真。真实气动手感放在飞行上，作战系统做得好玩即可。这样既保住"硬核"卖点，又不至于把 v1 做成无底洞。

## 3. 核心玩法循环（单关）

```
简报/Briefing → 起飞 → 完成任务目标(拦截/护航/击毁) → 结算/Debrief
                    ↑                                          │
                    └────────── 死亡/失败 → 重试 ─────────────┘
```

- 目标以文本 + 雷达标记给出；达成则推进，任务间有天气/对手强度差异。
- 结算界面：命中率、任务时间、剩余弹药、评价等级（S/A/B/C）。

## 4. 飞行模型规格（硬核节选）

Godot 用 `RigidBody3D`，在 `_integrate_forces()` 逐帧施加力：

| 项 | 设计 |
|---|---|
| 推力 | 节流阀驱动；加力燃烧室（AFB）临时大推力+代价 |
| 升力 | 按攻角(α)升力系数曲线：线性区 → 失速后急剧下降 |
| 阻力 | 寄生阻力(速度²) + 诱导阻力(攻角相关) |
| 姿态 | 舵面产生俯仰/横滚/偏航力矩，带阻尼项 |
| 失速 | α 超界或速度过低 → 升力崩溃、抖振视觉、操纵失效率 |
| G 限制 | 结构限 G → 超限损伤；人体限 G → 黑视/红视特效 |
| 能量管理 | 高空俯冲换速、机动掉速，逼玩家管油门 |

调试手段：开发期可显示 α/β、G、能量状态面板（cheat overlay）。

## 5. 战斗系统规格

| 武器 | 机制 |
|---|---|
| 机炮 | 无跟踪、高射速、弹药有限、过热惩罚 |
| 导弹 | 前置角锁定 → HUD 锁定框 + 音调 → 发射自行追击；制导有脱锁距离/角度限制 |
| 干扰弹 | 热焰引偏红外弹、铝箔引偏雷达弹，数量有限 |

命中判定：子弹/导弹射线或胶囊碰撞体 vs 敌机包围盒（简单碰撞体、复杂视觉 —— 见 3d-games 原则）。
导弹发射有最小距离保险丝，直迎头不打。

## 6. 敌机 AI（按复杂度选型）

- 基础巡逻/扑向玩家：**有限状态机**（巡逻→接战→规避→脱离）
- BOSS 级：多阶段行为（远距导弹压制 → 近距缠斗 → 一次性大招），用状态机 + 参数桌实现
- 难度参数作配置常量，便于平衡

## 7. 架构（Godot 4 场景树）

```
project.godot
scenes/
  menu/              主菜单、设置、关于
  briefing/          任务简报
  mission01..03/     任务场景（含关卡逻辑）
  hud/               HUD (CanvasLayer)
  player/
    player.tscn      RigidBody3D + MeshInstance + 武器挂点
    flight_controller.gd  飞行模型（核心逻辑，纯函数可测）
    weapons.gd       武器/弹药/过热
    damage.gd        损伤模型
  enemy/             敌机场景 + ai 状态机
  weapons/           子弹 / 导弹 / 干扰弹（对象池）
  fx/                爆炸/尾焰/导弹尾迹粒子 (GPUParticles3D)
assets/
  models/  textures/  audio/     （CC0 素材）
scripts/   （共享工具：对象池、信号总线、配置）
```

- **对象池**：子弹/导弹/粒子高频创建销毁 → 池化（game-development 热循环反模式）。
- **事件**：信号总线解耦（命中→HUD、损伤→音效）。
- **输入抽象**：action 映射键位+手柄（pc-games 原则），默认 WASD/方向键 + 摇杆＝俯仰横滚，Q/E 偏航，Shift 油门，等，可改。

## 8. 美术与音频

- 模型：Quaternius / Kenney 等 **CC0 模型包**（玩家战机 + 敌机 + 老式机），我来下载导入；先用占位几何体把玩法跑通，再整体替换。
- 粒子：机炮曳光、导弹尾焰、爆炸、引擎尾烟 → GPUParticles3D，GPUShader 做爆炸色散。
- 音效：免费素材库起步 + 程序化合成（引擎轰鸣用噪声+滤波），后续替换外包音效。
- 画质：SSAO + 辉光 + 晴空体积雾 + ACES 色调映射，标机性能档；提供低画质档位。

## 9. 文件与目录

```
3.super_plane/
├── design.md
├── plan.md
├── review.md          （由流程生成）
├── final_report.md    （由流程生成）
├── project/            Godot 项目根（.godot 除外，全部可版本化）
└── tools/              下载/导入素材的脚本
```

## 10. 里程碑（TDD 化拆分依据）

1. M1 工程骨架：空 Godot 项目 + 飞行模型可飞（无画面，HUD 数字显示）
2. M2 可视化：占位模型 + 相机跟随 + HUD 完整
3. M3 战斗：机炮/导弹/干扰弹 + 敌机 AI + 损伤
4. M4 关卡：任务流程 + 结算 + 菜单
5. M5 打磨：真实模型替换、粒子、音效、难度平衡、画质档
6. M6 发布：导出与手感 QA

## 11. 待确认 / 风险

1. ✅ **已确认（2026-08-17）**：硬核气动 + 游戏化作战；完整航电系统仿真 → 移入 v2 候选。
2. 免费模型包的具体选型需联网下载，确定后锁定 URL（Quaternius/Kenney 直链）。
3. ✅ **Godot MCP 已接入（2026-08-17）**：用户级配置 + 本项目 `addons/godot_mcp` 插件已装入并启用；编辑器 4.7.1 已能连通。路由：本会话 `claude --continue` 重启后接管。
4. 硬核飞行调参需要"手感迭代"，预留大量试飞调参时间。

---

> **已签字确认（2026-08-17）** ✅  v1 范围按文档执行。